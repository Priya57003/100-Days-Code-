
	        if(i!=T)
	        {
	            if(st.isEmpty())
	            {
	                st.push(arr[i]); //1
	            }
	            else(st.peek()<arr[i]) //1<2<4
	            {
	                
	                System.out.print(st.pop());
	                
	            }
	            
	        }
